# `trello version`

Print build metadata in the standard success envelope.

## Example

```bash
trello version --pretty
```

## Response

```json
{
  "ok": true,
  "data": {
    "version": "dev",
    "commit": "unknown",
    "date": "unknown"
  }
}
```
